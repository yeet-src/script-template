# Generated by example/bootstrap. Build container for test,
# launched by scripts/installers/macos.sh build_container().
# Tag: test:latest
FROM debian:13-slim

RUN apt-get update && apt-get install -y --no-install-recommends \
        clang \
        llvm \
        bpftool \
        libbpf-dev \
        linux-libc-dev \
        libelf-dev \
        zlib1g-dev \
        make \
        pkg-config \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /workspace
CMD ["bash"]
